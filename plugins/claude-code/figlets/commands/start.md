---
description: Start the Figlets designer workflow for your Figma design system.
---

Help me with my Figma design system using Figlets.

Call the `figlets_start` MCP tool first. If I already stated a concrete goal, call `figlets_route_intent`, then `figlets_workflow_guide`, and reply using the routed `designerResponse` instead of showing the generic menu. If routing returns `selectionPrompt`, use a single-choice selection UI when available; otherwise render `selectionPrompt.message`. Use `figlets_start.designerResponse` only for generic help/start requests.

Preserve the capability-menu shape when the generic menu is appropriate, and do not offer developer-mode options (no repo editing, no plugin editing, no raw Figma authoring).

For any design-system review, check, audit, setup-gap investigation, or contrast investigation, use only the Figlets workflow plus the Figlets MCP tools/scripts named by `figlets_workflow_guide`. Do not create custom scripts, inspect local snapshots, parse `tool-results`, read MCP transcript files, or use raw Figma APIs to perform the designer-facing review unless I explicitly ask you to go out of bounds.

For new design system setup, treat my prompt as initial direction, not a complete spec. Ask exactly one targeted intake question per assistant turn before calling `prepare_ds_config` or asking to build. If I answer multiple topics at once, record those answers and ask only the next single missing question. Do not ask a vague color-family/background-foreground pairing question by default after brand colors and semantic naming grammar are provided; infer reasonable generated pairings unless I ask for custom pairings. Do not draft a full proposal, palette, typography stack, grid defaults, or token names before intake. You may offer lightweight multiple-choice options, but ask questions before suggesting concrete token values unless I explicitly ask for suggestions. For setup contrast failures, use exact evaluated Figlets repair options when they exist, including paired `suggestedBackground` + `suggestedText` aliases; when no exact repair options exist, rerun `prepare_ds_config` once and do not ask me to approve prose-only directions. Treat "go for Figma" / "build it" as approval only when the latest `readyToBuild` is true.

Treat bulk design-system updates as Figlets scope when a structured, designer-approved payload exists. Inspect first. Pass `repairPlan.applyInput` to `repairPlan.tool` after approval; for setup repairs, pass the exact `inspect_ds_setup_gaps.repairPlan.applyInput` object to `apply_ds_setup_repairs`. Never replace `aliases` with counts, summaries, booleans, or prose-derived values. If only a subset is approved, filter entries while preserving each `aliases` object unchanged. If schema validation rejects a setup repair payload, stop, rerun `inspect_ds_setup_gaps`, and copy or filter the fresh structured `repairPlan.applyInput` instead of retrying invented arguments. Present `repairPlan.optionalApplyInput` as optional bulk creation. Use `inspect_ds_token_gaps` and `update_ds_tokens` for config-backed token completion; use `qa_binding_audit` read-only first, then `qa_binding_audit({ fix: true })` only for `fixableNow` after reading `byFixability`; use `plan_ds_figma_operations` for exact designer-specified variable, collection, mode, style, binding, metadata, and token lifecycle edits. Do not create tokens from binding-audit findings unless exact variable details are provided and validated. If no specialized planner exists, first check whether `plan_ds_figma_operations` can represent the exact designer-specified request; do not convert health-check findings into invented generic operations. Only product-specific planning gaps should be future Figlets planner scope. Do not end at "the gaps cannot be fixed", and do not write ad hoc scripts to compensate.

If `figlets_start` is not available, stop and tell me Figlets is not connected yet. Offer to install the Figlets Claude Code plugin by running `figlets-mcp setup --hosts=claude-code-plugin --yes` after my approval, or show that command so I can run it myself. Tell me to restart Claude Code afterwards, then ask again. Do not approximate the flow with raw Figma tools.
