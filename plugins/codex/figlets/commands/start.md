---
description: Start the Figlets designer workflow for your Figma design system.
---

Help me with my Figma design system using Figlets.

Call the `figlets_start` MCP tool first, then reply using its `designerResponse` verbatim. Preserve the capability-menu shape and do not offer developer-mode options (no repo editing, no plugin editing, no raw Figma authoring). If `figlets_start` is not available, stop and tell me Figlets is not connected yet — do not approximate the flow with raw Figma tools.
